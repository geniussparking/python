元组和列表可以相互转换；
定义一个列表可以使用list()：
l1 = list([1,2,3,4,])
也可以：
l1 = list((1,2,3,4,)) #实质上，这里是将一个元组(1,2,3,4,)进行了转换；
tuple()也一样
tu = tuple([1,2,3,4,])
或者：
tu = tuple((1,2,3,4,))
元组的方法：
tu = tuple((1,2,3,4,5,2,2,3))
print(tu.count(2))
print(tu.index(4))   # 这里的4是指的元素4，index(4)是找出4这个元素的索引号是多少
输出：
3
3


